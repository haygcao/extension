# OpenClip Sync — 代码剥离、多维数据同步(书签/剪贴板)与全新重构计划书

## 📌 一、 重构背景与终极产品愿景

当前项目正从原开源项目 `clipboard-history` 演进为一款**全能型去中心化浏览器工作区同步插件**。

通过引入对 [konode (konabe-studio/konode)](https://github.com/konabe-studio/konode.git) 核心能力的融合与合并，本项目将打破单一剪贴板插件的局限，实现**“一个插件解决剪贴板 + 书签 + 打开标签页多端同步”**的终极体验。同时，借此功能重构彻底清洗掉原开源项目的遗留冗余与版权隐患，打造一个全新的独立产品。

1. **逻辑与交互不一致**：原项目依赖第三方 InstantDB 云服务及复杂的上下文代理，交互繁琐混乱，包含大量废弃的第三方教程与臃肿布局。
2. **多插件功能大一统 (All-in-One)**：融合 **书签 (Bookmarks)**、**剪贴板历史 (Clipboard)**、**打开标签页 (Tabs)** 与 **规则配置** 的统一 WebDAV / 云盘无损同步，一个插件替代多个独立插件。
3. **数据库兼容性与极致轻量化**：**保留数据库接口解耦能力**（为未来可能接入 SQLite WASM / 自建 Lightweight API 预留插件化插槽），但放弃笨重的第三方 SDK，采用**零内存常驻、最轻量**的抽象层架构。
4. **版权与自主可控风险**：通过多模态架构升级与 Fresh UI 重写，实现 **100% 代码剥离与全新独立产权 (Fresh Rewrite)**。

---

## 🎯 二、 旧代码剥离与清理清单 (Removal Checklist)

### 1. 彻底移除的依赖与旧文件
- [ ] **InstantDB 残余文件**：彻底删除 `instant.schema.ts`，从 `package.json` 中移除 `@instantdb/core` 和 `@instantdb/react` 依赖。
- [ ] **冗余状态与 Context**：移除 `popup/contexts/` 下的 `EntriesContext.tsx`、`EntryIdToTagsContext.tsx`、`FavoriteEntryIdsContext.tsx`、`PinnedEntryIdsContext.tsx` 繁复包装，改为统一的数据流 Hook。
- [ ] **臃肿设置组件**：废弃当前的 `popup/components/modals/SettingsModalContent.tsx`（1300 行），全新编写轻量化 Settings 模块。
- [ ] **废弃图标与链接**：清空所有指向原作者网站、应用商店、引导页面的残留。

---

## 🔗 三、 融合 `konode` 的多维同步架构设计 (Multi-Module Sync)

我们将合并 `konode` 的数据调度理念，将扩展升级为多模块并行驱动架构：

```
                    ┌─────────────────────────┐
                    │  OpenClip Core Engine   │
                    └────────────┬────────────┘
                                 │
     ┌───────────────────────────┼───────────────────────────┐
     ▼                           ▼                           ▼
📋 剪贴板历史模块            🔖 浏览器书签模块           🌐 标签页与配置模块
(Clipboard Sync)           (Bookmarks Sync)           (Tabs & Settings Sync)
  - 历史/置顶/收藏            - 目录树双向合并           - 当前窗口标签页备份
  - 敏感词黑名单              - 去重与失效清理           - 设备规则同步
     │                           │                           │
     └───────────────────────────┼───────────────────────────┘
                                 ▼
                    🗄️ 统一 WebDAV / 多云存储引擎
```

### 1. 核心大一统功能点：
1. **📋 剪贴板历史同步**：多设备历史记录、自定义标签、固定置顶、敏感词全词自动清理。
2. **🔖 书签树无感同步 (来自 konode 融合)**：自动跨设备同步与去重合并 Chrome/Edge 书签树，用户无需再安装单独的书签同步插件。
3. **🌐 打开标签页 / 会话备份**：支持一键备份发送当前已打开的标签页分组至云端及其他设备。
4. **⚙️ 规则与设备注册表**：全端共享自动清理规则、过滤黑名单与 discoveredDevices 设备索引。

---

## 🗄️ 四、 轻量级 DB 抽象层设计 (Lightweight DB Adapter)

针对后续可能的数据库扩展，我们将建立一套**极低内存开销、高度解耦**的 DB 适配器架构：

```ts
export interface OpenClipDbAdapter {
  name: string;
  isAvailable(): Promise<boolean>;
  getEntries(query?: Record<string, unknown>): Promise<Entry[]>;
  saveEntries(entries: Entry[]): Promise<void>;
  deleteEntries(ids: string[]): Promise<void>;
}
```

- **默认实现**：`LocalIndexedDbAdapter`（基于浏览器原生 IndexedDB，内存占用接近零，无需加载任何大型云端 SDK）。
- **预留插槽**：`RemoteDbAdapter`（未来可无缝插拔 SQLite WASM 或自建 API，绝不影响前端 UI 与同步业务逻辑）。

---

## 🎨 五、 三态 UI 与人体工学交互重构 (Fresh UX Design)

不再照搬原作者混乱的交互逻辑，针对扩展的三种常见形态（Popup / Floating Window / SidePanel）进行极致方便好用的定制：

```
┌─────────────────────────────────────────────────────────────┐
│ 🚀 OpenClip Sync  [v2.3.1]               [同步状态: 🟢] [⚙️]  │
├─────────────────────────────────────────────────────────────┤
│ 🔍 [ 搜索剪贴板 / 书签 / 标签...                   ] [🧹]   │
├─────────────────────────────────────────────────────────────┤
│  [📋 剪贴板]    [🔖 浏览器书签]    [🌐 多端云同步 (2台设备)] │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 📌 示例剪贴板内容 A               [仅拉取此设备] [复制] │ │
│ │ 🏷️ #Code  🕒 2分钟前  💻 办公电脑                        │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 1. 三种形态的精细化 UX 定制
1. **Popup 快捷弹窗模式（默认 400x560）**：弹出时搜索框自动聚焦；按 `Enter` 快捷复制；按 `Esc` 关闭。
2. **Floating 独立悬浮窗模式 (窗口化常驻)**：自由拉伸大小；顶部窗口置顶按钮；支持剪贴板与书签拖拽。
3. **SidePanel 侧边栏模式 (Chrome 侧栏伴随)**：纵向伴随工作流；一键填充剪贴板文本或快速打开同步书签。

---

## 🚀 六、 分阶段实施路线图 (Phase-by-Phase Roadmap)

| 阶段 | 实施内容 | 目标状态 |
| :--- | :--- | :--- |
| **Phase 1: 瘦身剥离与 DB 抽象** | 卸载 `@instantdb` 依赖，重构 `utils/db/core.ts` 为最轻量 `OpenClipDbAdapter` 模式。 | 内存占用降至最低，为未来扩展留出无缝插槽 |
| **Phase 2: 融合 Bookmarks/konode 模块** | 编写 `bookmarks` 同步驱动，将书签树接入 WebDAV / 云端统一同步流。 | 实现“剪贴板 + 书签”一插件双同步 |
| **Phase 3: Fresh Settings & UI 重写** | 重构 `popup/App.tsx` 与 Settings 模块，提供卡片化多模块切片与三态交互体验。 | 界面焕然一新，彻底消除原项目残留 |
| **Phase 4: 终极审查** | 全局清理无用 i18n 键、废弃类型与遗留注释，完成 100% 干净打包。 | 具备完全独立的自主知识产权 |

---

> 💡 **备注**：计划书已更新完成，已将 `konode` 的“书签 + 剪贴板多合一”大一统愿景写入 Plan。后续代码修改将按此方案逐步落地。
