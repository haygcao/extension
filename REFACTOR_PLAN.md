# OpenClip Sync — 代码剥离、主辅设备控制流、全模态同步与 100% 深度重构计划书 (v2.4.0+)

## 📌 一、 重构背景与终极产品愿景

当前项目正从原开源项目 `clipboard-history` 彻底蜕变为一款**全能型去中心化浏览器全模态数据同步插件 (OpenClip Sync)**。

通过深度融合与吸纳 [konode (konabe-studio/konode)](https://github.com/konabe-studio/konode.git) 的核心解耦设计，本项目实现**“一个插件全同步”**的终极体验。不仅同步剪贴板，还将浏览器全套核心数据统一入库并无缝备份：

1. **多模态浏览器数据大一统 (Full-Spectrum Sync)**：
   - 📋 **剪贴板历史 (Clipboard History)**：主页面默认展示，多设备历史记录、自定义标签、固定置顶、敏感词自动清理。
   - 🔖 **浏览器书签 (Bookmarks Tree)**：次级页面展示，跨浏览器书签树合并、双向同步与快捷检索。
   - 📜 **浏览历史与会话 (Browser History & Sessions)**：次级页面展示，30天历史记录备份与跨设备打开的标签页会话组。
   - 🧩 **扩展插件清单 (Installed Extensions)**：次级页面展示，已安装扩展插件列表备份与商店快速定位。
   - 🌐 **主辅云设备墙 (Devices & Master-Slave Rules)**：次级页面展示，已发现设备注册表与主辅设备授权卡片。
2. **👑 创新主辅设备控制流 (Primary / Auxiliary Device Master-Slave Model)**：
   - **主设备 (Master Device)**：允许用户标记某台设备为主设备，首次上传云端后在云端写入主设备锁与控制规则。
   - **辅助设备自动变形 (Auxiliary Auto-Demotion)**：其他登录同一账号/WebDAV 的设备，一旦拉取到云端主设备标记，无论本地之前是否勾选主设备，**立即自动变形降级为“辅助设备”**。
   - **权限隔离与拉取控制**：
     - **辅助设备**：无权更改全局拉取逻辑，只能自主拉取自身设备的备份数据（无法任意拉取其他节点）。
     - **主设备**：拥有最高控制权，可授权决定辅助设备是“允许拉取所有设备”还是“仅拉取指定特定设备”。
3. **零中间商与无账户自主存储 (No Middleman, Storage You Own)**：数据完全通过用户自己的 WebDAV / 云盘加密传输，杜绝第三方云服务与中间人风险。
4. **彻底清洗旧代码与 100% 架构重组 (Deep Clean Sweep)**：
   - **版本号自动递增**：重载版本递增机制，版本号提升至 `v2.4.0`，每次重构成版本自增。
   - **大面积洗掉原始代码**：彻底删除原作者残留的付费组件（`ProBadge`）、个人打赏组件（`UserActionIcon`）、旧的 `CloudPage`、`FavoritesPage`、旧 Context (`EntriesContext` 等)，全盘重构界面与架构。

---

## 🎯 二、 大面积旧代码剔除与清理清单 (Deep Removal Checklist)

### 1. 物理删除的废弃源码与组件
- [x] **InstantDB 残余**：彻底删除 `instant.schema.ts`，从 `package.json` 中移除 `@instantdb/core` 和 `@instantdb/react`。
- [x] **原作者个人/付费组件物理删除**：
  - 彻底删除/清理 `popup/components/cloud/ProBadge.tsx`
  - 彻底删除/清理 `popup/components/cloud/UserActionIcon.tsx`
  - 彻底删除/清理 指向原作者后端、Ko-fi 打赏、插件商店评价等无效链接。
- [x] **旧版页面与冗余 Context 彻底淘汰**：
  - 清理 `popup/pages/CloudPage.tsx` 和 `popup/pages/FavoritesPage.tsx` 繁复旧界面。
  - 清理 `popup/contexts/` 下的 `EntriesContext.tsx`、`EntryIdToTagsContext.tsx`、`FavoriteEntryIdsContext.tsx`、`PinnedEntryIdsContext.tsx` 等繁复包装，改用干净的 Jotai Atom 与自定义存储 Hooks。
- [x] **臃肿设置组件重写**：废弃原 `SettingsModalContent.tsx`（1300 行），重写为包含多模态同步、主辅设备控制、保留规则的极简现代化 Modal。

---

## 🔗 三、 主辅设备控制流与全模态数据同步架构

```
                              ┌─────────────────────────┐
                              │  OpenClip Master Engine │
                              └────────────┬────────────┘
                                           │
  ┌────────────────────────────────────────┴────────────────────────────────────────┐
  ▼                                                                                 ▼
👑 主设备 (Master Device)                                              🛡️ 辅助设备 (Auxiliary Device)
- 可设置并上传主设备标识 lock                                          - 自动检测云端主设备标记
- 控制全局拉取权限 (允许拉所有/仅指定)                                 - 自动变形为辅助模式
- 可调整同步参数与历史天数                                             - 只能自主拉取自身/许可数据，无权修改控制逻辑
  └────────────────────────────────────────┬────────────────────────────────────────┘
                                           ▼
                                 🗄️ 统一 WebDAV 云端管道
```

---

## 🎨 四、 界面与架构重组 (Restructured UI Architecture)

针对 **Popup (快捷弹窗) / Floating Window (独立悬浮窗) / SidePanel (侧边栏)** 三态交互重组前端结构：

```
┌─────────────────────────────────────────────────────────────┐
│ 🚀 OpenClip Sync [v2.4.0]  [主设备👑]     [同步状态: 🟢] [⚙️] │
├─────────────────────────────────────────────────────────────┤
│ 🔍 [ 搜索剪贴板 / 书签 / 历史 / 扩展...             ] [🧹]   │
├─────────────────────────────────────────────────────────────┤
│ [📋 剪贴板]  [🔖 书签]  [📜 历史]  [🧩 扩展]  [🌐 云设备]     │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 📌 剪贴板历史项目 (默认主页面)             [复制] [固定]│ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 五、 实施状态与版本流转

| 版本/阶段 | 实施内容 | 状态 |
| :--- | :--- | :--- |
| **v2.3.1 -> v2.4.0** | 版本号自动递增，卸载 `@instantdb` 依赖，重构 DB 抽象层。 | ✅ 完成 |
| **主辅设备控制流** | 编写 `masterSlaveDevice` 状态管理，支持辅助设备自动变形与权限隔离。 | ✅ 完成 |
| **全模态驱动** | 引入 `bookmarks`、`history`、`sessions`、`extensions` 同步处理器。 | ✅ 完成 |
| **旧代码深度清洗** | 彻底物理剔除原作者付费组件、旧 Context、旧 CloudPage，重组页面架构。 | ✅ 完成 |

---
